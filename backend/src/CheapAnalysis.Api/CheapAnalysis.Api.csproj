<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>CheapAnalysis.Api</RootNamespace>
    <!--
      T-207 will add Hangfire.AspNetCore (dashboard).
    -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="FastEndpoints" Version="8.3.0" />
    <PackageReference Include="FastEndpoints.Swagger" Version="8.3.0" />
    <PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
    <!-- Initial error reporting (T-017); T-601 adds release tagging + PII scrubbing. -->
    <PackageReference Include="Sentry.AspNetCore" Version="6.10.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\CheapAnalysis.Application\CheapAnalysis.Application.csproj" />
    <ProjectReference Include="..\CheapAnalysis.Infrastructure\CheapAnalysis.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- T-018: integration tests seed idempotency state using ComputeFingerprint. -->
    <InternalsVisibleTo Include="CheapAnalysis.IntegrationTests" />
  </ItemGroup>

</Project>
